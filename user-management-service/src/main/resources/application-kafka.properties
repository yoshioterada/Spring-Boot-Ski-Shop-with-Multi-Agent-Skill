# Kafka Event Publishing & Consuming
# Activate with: spring.profiles.active=kafka

# Spring Cloud Stream - Kafka Binder
spring.cloud.stream.kafka.binder.brokers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
spring.cloud.stream.kafka.binder.auto-create-topics=false
spring.cloud.stream.kafka.binder.replicationFactor=${KAFKA_REPLICATION_FACTOR:3}

# Input Binding - consume user-events (USER_REGISTERED from auth-service)
spring.cloud.stream.bindings.userEventConsumer-in-0.destination=user-events
spring.cloud.stream.bindings.userEventConsumer-in-0.group=user-management-service
spring.cloud.stream.bindings.userEventConsumer-in-0.content-type=application/json
spring.cloud.stream.kafka.bindings.userEventConsumer-in-0.consumer.autoCommitOffset=true
spring.cloud.stream.kafka.bindings.userEventConsumer-in-0.consumer.startOffset=latest

# Output Binding - publish domain-events (mapped to notification-events topic due to Event Hub 10-topic limit)
spring.cloud.stream.bindings.domainEvents-out-0.destination=notification-events
spring.cloud.stream.bindings.domainEvents-out-0.content-type=application/json

# Azure Event Hubs SASL/SSL Configuration
spring.cloud.stream.kafka.binder.configuration.security.protocol=SASL_SSL
spring.cloud.stream.kafka.binder.configuration.sasl.mechanism=PLAIN
spring.cloud.stream.kafka.binder.configuration.sasl.jaas.config=org.apache.kafka.common.security.plain.PlainLoginModule required username="$ConnectionString" password="${EVENTHUB_CS}";
